/**
 * Cantidad de desafíos de código.
 *
 * Se declara acá y no como `challenges.length` porque la pantalla de inicio
 * necesita el número, y tocar el banco desde ahí arrastraría los desafíos y el
 * intérprete de Java al bundle inicial. Un test verifica que este valor
 * coincida con la cantidad real, así que no puede quedar desactualizado.
 */
export const CHALLENGE_COUNT = 20
